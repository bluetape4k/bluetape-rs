[package]
name = "bluetape-rs"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
description = "Rust backend primitives for the bluetape ecosystem."
license = "MIT"
repository = "https://github.com/bluetape4k/bluetape-rs"
readme = "README.md"
keywords = ["backend", "validation", "logging", "testing", "rust"]
categories = ["development-tools", "asynchronous"]

[workspace]
members = [
    "crates/core",
]
resolver = "3"

[workspace.package]
edition = "2024"
rust-version = "1.85"
license = "MIT"
repository = "https://github.com/bluetape4k/bluetape-rs"

[workspace.dependencies]
bluetape-rs-core = { path = "crates/core", version = "0.1.0" }

[lib]
name = "bluetape_rs"
path = "src/lib.rs"

[features]
default = ["core"]
core = ["dep:bluetape-rs-core"]

[dependencies]
bluetape-rs-core = { workspace = true, optional = true }
