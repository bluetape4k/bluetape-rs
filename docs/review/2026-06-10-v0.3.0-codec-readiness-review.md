# v0.3.0 코덱 릴리스 준비 검토

날짜: 2026-06-10

범위:

- `CHANGELOG.md`
- `README.md`
- `README.ko.md`
- `WIP.md`
- `docs/release/release-guide.md`
- `docs/release/2026-06-10-v0.3.0-codec-readiness.md`
- `docs/lessons/2026-06-10-v0.3.0-codec-readiness.md`
- Pull request #68

기준점:

- `origin/develop`: `639b030559b20960e0f3d79a8e3b1cf5060994dd`

## 7-Tier 검토

| Tier | 초점 | 결과 | 메모 |
|---|---|---:|---|
| 1 | Release 범위 | 통과 | #58은 readiness-only로 유지하며 tag, publish, GitHub Release, `main` promote를 실행하지 않습니다. |
| 2 | Version 및 package metadata | 통과 | `bluetape-rs-codec`는 `0.3.0`이고 root facade는 `codec` feature를 통한 optional 상태입니다. |
| 3 | 문서 정확성 | 통과 | README, changelog, WIP, release guide, readiness note가 현재 branch 상태와 일치합니다. |
| 4 | 보류 범위 | 통과 | Compression은 `0.4.0`, serialization은 `0.5.0`에 남아 있습니다. |
| 5 | Test 및 품질 근거 | 통과 | 로컬 품질 기준과 coverage gate가 통과했습니다. |
| 6 | Release 위험 | 통과 | Publish 근거는 dry-run뿐이며 stable release 실행은 분리되어 있습니다. |
| 7 | PR 준비 | 통과 | PR #68은 `develop`을 대상으로 하고 milestone `0.3.0`, clean merge state, `## DoD Status`인 마지막 section을 갖습니다. |

## 발견 사항

- P0: 0
- P1: 0
- P2: 0
- P3: 0

최종 blocker 수: `P0=0 P1=0`

## 검증 근거

- `git diff --check` 통과
- `cargo fmt --all --check` 통과
- `cargo test -p bluetape-rs-codec --all-features --locked` 통과
- `cargo test --workspace --all-features --locked` 통과
- `cargo clippy --workspace --all-targets --all-features --locked -- -D warnings` 통과
- `RUSTDOCFLAGS="-D warnings" cargo doc --workspace --all-features --no-deps --locked` 통과
- `cargo publish -p bluetape-rs-codec --dry-run --locked` 통과; dry-run이므로 upload를 중단
- Homebrew cargo 환경에서 `LLVM_COV` 및 `LLVM_PROFDATA` 경로를 명시한 뒤 `cargo llvm-cov --workspace --all-features --locked --lcov --output-path coverage/lcov.info` 통과
- `git diff --check`, `cargo fmt --all --check`, `cargo test -p bluetape-rs-codec --all-features --locked`, `RUSTDOCFLAGS="-D warnings" cargo doc --workspace --all-features --no-deps --locked`를 사용한 README parity refresh 통과
- `gh pr view 68 --json number,title,url,body,baseRefName,headRefName,mergeStateStatus,labels,milestone`에서 PR이 `develop`을 대상으로 하고 milestone `0.3.0`, merge state `CLEAN`, 마지막 section `## DoD Status`를 가진 body임을 확인
- `gh pr diff 68 --name-only`에서 PR diff가 release-readiness 문서로 제한됨을 확인
- `gh pr checks 68`에서 초기 PR 검증 시 branch에 아직 check가 없음을 보고
