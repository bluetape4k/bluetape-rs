# v0.3.0 코덱 릴리스 준비 상태 검토

날짜: 2026-06-10

범위:

- `CHANGELOG.md`
- `README.md`
- `README.ko.md`
- `WIP.md`
- `docs/release/release-guide.md`
- `docs/release/2026-06-10-v0.3.0-codec-readiness.md`
- `docs/lessons/2026-06-10-v0.3.0-codec-readiness.md`
- PR #68

기준선:

- `origin/develop`: `639b030559b20960e0f3d79a8e3b1cf5060994dd`

## 7-Tier 검토

| 단계 | 초점 | 결과 | 비고 |
|---|---|---:|---|
| 1 | 릴리스 범위 | Pass | #58은 준비 상태만 다루며 태그, 게시, GitHub Release, `main` 승격은 없다. |
| 2 | 버전 및 패키지 메타데이터 | Pass | `bluetape-rs-codec`은 `0.3.0`이고 루트 파사드는 `codec` feature를 통해 선택적으로 유지된다. |
| 3 | 문서 정확성 | Pass | README, 변경 로그, WIP, 릴리스 가이드, 준비 상태 메모가 현재 브랜치 상태와 일치한다. |
| 4 | 보류 범위 | Pass | 압축은 `0.4.0`, 직렬화는 `0.5.0`에 남는다. |
| 5 | 테스트 및 품질 근거 | Pass | 로컬 품질 기준과 커버리지 게이트가 통과했다. |
| 6 | 릴리스 위험 | Pass | 게시 근거는 dry-run뿐이며 안정 릴리스 실행은 별도다. |
| 7 | PR 준비 상태 | Pass | PR #68이 `develop`을 대상으로 하고 마일스톤 `0.3.0`을 가지며 병합 상태가 CLEAN이고 마지막 섹션이 `## DoD Status`다. |

## 발견 사항

- P0: 0
- P1: 0
- P2: 0
- P3: 0

최종 차단 요소 수: `P0=0 P1=0`

## 검증 근거

- `git diff --check` 통과.
- `cargo fmt --all --check` 통과.
- `cargo test -p bluetape-rs-codec --all-features --locked` 통과.
- `cargo test --workspace --all-features --locked` 통과.
- `cargo clippy --workspace --all-targets --all-features --locked -- -D warnings` 통과.
- `RUSTDOCFLAGS="-D warnings" cargo doc --workspace --all-features --no-deps --locked` 통과.
- `cargo publish -p bluetape-rs-codec --dry-run --locked` 통과. dry run이므로
  upload는 중단했다.
- Homebrew cargo 환경에서 `LLVM_COV` 및 `LLVM_PROFDATA` 경로를 명시한 뒤
  `cargo llvm-cov --workspace --all-features --locked --lcov --output-path coverage/lcov.info`가
  통과했다.
- README 동등성 갱신이 `git diff --check`, `cargo fmt --all --check`,
  `cargo test -p bluetape-rs-codec --all-features --locked` 및
  `RUSTDOCFLAGS="-D warnings" cargo doc --workspace --all-features --no-deps --locked`와
  함께 통과했다.
- `gh pr view 68 --json number,title,url,body,baseRefName,headRefName,mergeStateStatus,labels,milestone`로
  PR 대상이 `develop`, 마일스톤이 `0.3.0`, 병합 상태가 `CLEAN`이며 본문 마지막
  섹션이 `## DoD Status`임을 확인했다.
- `gh pr diff 68 --name-only`로 PR diff가 릴리스 준비 문서로 한정됨을 확인했다.
- 초기 PR 검증 시점에 브랜치에 아직 check가 없음을 `gh pr checks 68`이 보고했다.
