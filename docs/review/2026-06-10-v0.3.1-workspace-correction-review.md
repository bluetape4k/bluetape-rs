# v0.3.1 워크스페이스 정정 검토

날짜: 2026-06-10
이슈: #70
브랜치: `issue-70-v0.3.1-workspace-release`
기반: `origin/develop`

## 범위

모든 현재 워크스페이스 크레이트를 `0.3.1`에 맞추고 공개 설치 문서를
갱신하며 완전한 워크스페이스 게시 릴리스 근거를 기록해 `0.3.0`
마일스톤 릴리스를 정정한다.

검토 파일:

- `Cargo.toml`
- `Cargo.lock`
- `crates/*/Cargo.toml`
- `README.md`
- `README.ko.md`
- `crates/*/README.md`
- `crates/codec/src/lib.rs`
- `CHANGELOG.md`
- `docs/release/2026-06-10-v0.3.1-workspace-correction.md`
- `docs/lessons/2026-06-10-workspace-release-version-alignment.md`

## Seven-Tier 게이트

| 단계 | 결과 | 근거 |
|---|---|---|
| 1. 릴리스 계약 | PASS | `v0.3.1`을 `0.3.0` 마일스톤의 완전한 워크스페이스 정정으로 문서화했다. |
| 2. 버전 메타데이터 | PASS | `cargo metadata --no-deps --format-version 1`이 7개 패키지 모두 `0.3.1`이라고 보고한다. |
| 3. 의존성 요구 사항 | PASS | 루트 `[workspace.dependencies]`가 모든 내부 크레이트를 `0.3.1`으로 가리킨다. |
| 4. 문서 | PASS | README 파일과 크레이트 README 파일이 `0.3.1` 의존성 조각을 사용한다. |
| 5. 검증 | PASS | fmt, 공백, 워크스페이스 테스트, clippy, rustdoc, 게시 dry-run이 통과했다. |
| 6. 게시 안전성 | PASS | `cargo publish --workspace --dry-run --locked --allow-dirty`가 모든 워크스페이스 크레이트를 패키징하고 검증했다. |
| 7. 회귀 위험 | PASS | 패키지 버전 메타데이터 assertion 외에 런타임 동작은 변경하지 않았다. |

## 발견 사항

P0 발견 사항 없음.

P1 발견 사항 없음.

P2: 없음.

P3: 없음.

P0=0 P1=0

## 검증 근거

- `cargo metadata --no-deps --format-version 1 | jq -r '.packages[] | "\(.name) \(.version)"' | sort`: PASS; 모든 패키지가 `0.3.1`이다.
- `rg -n "version = \"0\.(1\.1|2\.0|3\.0)\"|bluetape-rs(-[a-z]+)? = \"0\.(1\.1|2\.0|3\.0)\"|bluetape-rs = \{ version = \"0\.(1\.1|2\.0|3\.0)\"" Cargo.toml crates README.md README.ko.md`: PASS; 일치 항목 없음.
- `cargo fmt --all --check`: PASS.
- `git diff --check`: PASS.
- `cargo test --workspace --all-features --locked`: PASS.
- `cargo clippy --workspace --all-targets --all-features --locked -- -D warnings`: PASS.
- `RUSTDOCFLAGS="-D warnings" cargo doc --workspace --all-features --no-deps --locked`: PASS.
- `cargo publish --workspace --dry-run --locked --allow-dirty`: PASS; dry run이므로
  upload를 중단했다.

## DoD 상태

- [x] 버전 정렬 완료.
- [x] 공개 문서 갱신 완료.
- [x] 로컬 검증 통과.
- [x] Step 6-R 로컬 검토가 `P0=0 P1=0`으로 통과.
- [ ] PR 검토 통과.
- [ ] PR 병합.
- [ ] 태그, GitHub Release, crates.io 게시 완료.
