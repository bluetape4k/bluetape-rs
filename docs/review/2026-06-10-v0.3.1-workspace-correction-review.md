# v0.3.1 Workspace Correction Review

Date: 2026-06-10
Issue: #70
Branch: `issue-70-v0.3.1-workspace-release`
Base: `origin/develop`

## Scope

Correct the `0.3.0` milestone release by aligning every current workspace crate
to `0.3.1`, updating public installation docs, and recording release evidence
for a complete workspace publish.

Reviewed files:

- `Cargo.toml`
- `Cargo.lock`
- `crates/*/Cargo.toml`
- `README.md`
- `README.ko.md`
- `crates/*/README.md`
- `crates/codec/src/lib.rs`
- `CHANGELOG.md`
- `docs/release/2026-06-10-v0.3.1-workspace-correction.md`
- `docs/lessons/2026-06-10-workspace-release-version-alignment.md`

## Seven-Tier Gate

| Tier | Result | Evidence |
|---|---|---|
| 1. Release contract | PASS | `v0.3.1` is documented as the complete workspace correction for the `0.3.0` milestone. |
| 2. Version metadata | PASS | `cargo metadata --no-deps --format-version 1` reports all seven packages at `0.3.1`. |
| 3. Dependency requirements | PASS | Root `[workspace.dependencies]` points all internal crates to version `0.3.1`. |
| 4. Documentation | PASS | README files and crate README files use `0.3.1` dependency snippets. |
| 5. Validation | PASS | fmt, whitespace, workspace tests, clippy, rustdoc, and publish dry-run passed. |
| 6. Publish safety | PASS | `cargo publish --workspace --dry-run --locked --allow-dirty` packaged and verified all workspace crates. |
| 7. Regression risk | PASS | No runtime behavior changed except package version metadata assertion. |

## Findings

No P0 findings.

No P1 findings.

P2: None.

P3: None.

P0=0 P1=0

## Validation Evidence

- `cargo metadata --no-deps --format-version 1 | jq -r '.packages[] | "\(.name) \(.version)"' | sort`: PASS; all packages are `0.3.1`.
- `rg -n "version = \"0\.(1\.1|2\.0|3\.0)\"|bluetape-rs(-[a-z]+)? = \"0\.(1\.1|2\.0|3\.0)\"|bluetape-rs = \{ version = \"0\.(1\.1|2\.0|3\.0)\"" Cargo.toml crates README.md README.ko.md`: PASS; no matches.
- `cargo fmt --all --check`: PASS.
- `git diff --check`: PASS.
- `cargo test --workspace --all-features --locked`: PASS.
- `cargo clippy --workspace --all-targets --all-features --locked -- -D warnings`: PASS.
- `RUSTDOCFLAGS="-D warnings" cargo doc --workspace --all-features --no-deps --locked`: PASS.
- `cargo publish --workspace --dry-run --locked --allow-dirty`: PASS; upload aborted due to dry run.

## DoD Status

- [x] Versions aligned.
- [x] Public docs updated.
- [x] Local validation passed.
- [x] Step 6-R local review passed with `P0=0 P1=0`.
- [ ] PR review passed.
- [ ] PR merged.
- [ ] Tag, GitHub Release, and crates.io publish completed.
