# v0.1.1 릴리스 검토

이슈: #29
브랜치: `release/v0.1.1`
날짜: 2026-06-10

## 범위

`v0.1.1`을 위한 release metadata 준비입니다.

변경 파일:

- `Cargo.toml`
- `Cargo.lock`
- `crates/core/Cargo.toml`
- `crates/logging/Cargo.toml`
- `crates/test/Cargo.toml`
- `CHANGELOG.md`

## 검토

### 버전 일관성

통과. Workspace package, member crate, workspace path dependency,
`Cargo.lock` package entry가 `0.1.1`로 정렬되어 있습니다.

### Changelog

통과. `CHANGELOG.md`에 릴리스 범위와 검증 명령을 포함한
`## [v0.1.1] - 2026-06-10`이 있습니다.

### 동작 위험

통과. Release branch는 metadata와 changelog만 변경합니다. Rust source,
feature flag semantics, dependency version, 공개 API 동작은 변경하지
않았습니다.

### 검증

통과.

- `git diff --check`
- `cargo fmt --all --check`
- `cargo test --workspace`
- `cargo clippy --workspace --all-targets --all-features -- -D warnings`
- `cargo doc --workspace --no-deps`

## 발견 사항

P0/P1/P2/P3 발견 사항이 없습니다.

## 판정

PASS

P0 개수: 0
P1 개수: 0
