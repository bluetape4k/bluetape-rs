# v0.1.1 릴리스 검토

이슈: #29
브랜치: `release/v0.1.1`
날짜: 2026-06-10

## 범위

`v0.1.1` 릴리스 메타데이터 준비

변경 파일:

- `Cargo.toml`
- `Cargo.lock`
- `crates/core/Cargo.toml`
- `crates/logging/Cargo.toml`
- `crates/test/Cargo.toml`
- `CHANGELOG.md`

## 검토

### 버전 일관성

PASS. 워크스페이스 패키지, 멤버 크레이트, 워크스페이스 경로 의존성,
`Cargo.lock` 패키지 항목이 `0.1.1`에 맞춰져 있다.

### 변경 로그

PASS. `CHANGELOG.md`에 릴리스 범위와 검증 명령을 포함한
`## [v0.1.1] - 2026-06-10`이 있다.

### 동작 위험

PASS. 릴리스 브랜치는 메타데이터와 변경 로그만 변경한다. Rust 소스,
feature flag 의미론, 의존성 버전, 공개 API 동작은 변경하지 않았다.

### 검증

PASS.

- `git diff --check`
- `cargo fmt --all --check`
- `cargo test --workspace`
- `cargo clippy --workspace --all-targets --all-features -- -D warnings`
- `cargo doc --workspace --no-deps`

## 발견 사항

P0/P1/P2/P3 발견 사항 없음.

## 판정

PASS.

P0 개수: 0
P1 개수: 0
