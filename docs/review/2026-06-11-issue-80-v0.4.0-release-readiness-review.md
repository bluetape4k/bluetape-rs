# 이슈 #80 v0.4.0 릴리스 준비 검토

## 범위

- `0.4.0` workspace release를 위한 version 정렬
- 공개 README와 localized README의 release-prep 문구
- Changelog, WIP, release guide readiness 갱신
- Milestone #73 및 이슈 #80의 release 근거

## 검토 결과

P0=0 P1=0

Blocker 발견 사항이 없습니다. 이 변경은 release-prep 문서와 version 정렬에
관한 것이며 production Rust 동작은 변경하지 않았습니다.

## 근거

| 검사 | 상태 |
|---|---|
| GNO GitHub/docs retrieval | 통과 |
| Milestone issue scan | 통과 |
| crates.io latest-version scan | 통과 |
| Existing tag/release scan | 통과 |
| Workspace metadata | 통과 |
| `cargo fmt --all --check` | 통과 |
| `cargo test --workspace --all-features --locked` | 통과 |
| `cargo clippy --workspace --all-targets --all-features --locked -- -D warnings` | 통과 |
| `RUSTDOCFLAGS="-D warnings" cargo doc --workspace --all-features --no-deps --locked` | 통과 |
| `cargo publish --workspace --dry-run --locked --allow-dirty` | 통과 |
| `cargo publish --workspace --dry-run --locked` | 통과 |
| `git diff --check` | 통과 |

## DoD 상태

| 항목 | 상태 |
|---|---|
| Release-readiness 범위 검토 | 완료 |
| P0/P1 blocker 수 기록 | 완료 |
| PR 전에 로컬 validation 완료 | 완료 |
| Commit 후 clean publish dry-run 완료 | 완료 |
